--- a/dbus/dbus-server-launchd.c	2017-06-06 17:21:59.000000000 +0900
+++ b/dbus/dbus-server-launchd.c	2017-06-06 17:41:45.000000000 +0900
@@ -70,12 +70,15 @@
     DBusServer *server;
     DBusString address;
     int launchd_fd;
+    size_t launchd_fd_cnt = 1;
     launch_data_t sockets_dict, checkin_response;
     launch_data_t checkin_request;
     launch_data_t listening_fd_array, listening_fd;
     launch_data_t environment_dict, environment_param;
     const char *launchd_socket_path, *display;
 
+    launch_activate_socket("unix_domain_listener", &launchd_fd, &launchd_fd_cnt);
+
     launchd_socket_path = _dbus_getenv (launchd_env_var);
     display = _dbus_getenv ("DISPLAY");
 
@@ -104,6 +107,7 @@
         goto l_failed_0;
       }
 
+#if 0
     if ((checkin_request = launch_data_new_string (LAUNCH_KEY_CHECKIN)) == NULL)
       {
         dbus_set_error (error, DBUS_ERROR_NO_MEMORY,
@@ -155,6 +159,7 @@
 
     listening_fd = launch_data_array_get_index (listening_fd_array, 0);
     launchd_fd = launch_data_get_fd (listening_fd);
+#endif
 
     _dbus_fd_set_close_on_exec (launchd_fd);
 
